import React from 'react';
import {observer} from "mobx-react-lite";

const Home = observer(() => {
    return (
        <div>
            Домашняя страница
        </div>
    );
});

export default Home;